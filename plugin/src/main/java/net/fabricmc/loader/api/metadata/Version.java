package net.fabricmc.loader.api.metadata;

/** Adaptateur Irium — version (surface utilisée : getFriendlyString). */
public interface Version extends Comparable<Version> {

    String getFriendlyString();
}
